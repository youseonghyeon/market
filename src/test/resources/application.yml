spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/market_test
    username: postgres
    password: pass
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
